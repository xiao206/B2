# 基于银河麒麟操作系统的 AI 智能匹配与能力图谱系统（V2.0）  
## 前端架构设计方案（B组｜Spring Boot + Vue.js｜LoongArch + 银河麒麟 V11）  
**版本**：V2.0  
**日期**：2026 年 4 月  
**面向对象**：开发团队落地（前端）  

---

## 目录
0. 赛题要求深度对标（前端）  
1. 前端目标与边界  
2. 总体前端架构与技术选型  
3. 前端工程结构与规范  
4. 页面与功能模块设计（双角色 + 管理端）  
5. 路由、布局与权限体系（RBAC）  
6. API 接入层与数据模型（TypeScript）  
7. 文档上传与解析进度前端方案  
8. 能力图谱可视化方案（AntV G6 5.x）  
9. 智能匹配结果呈现方案（ECharts）  
10. 性能、兼容性与安全加固（信创浏览器）  
11. 构建、离线依赖与部署（Nginx）  
12. 测试、质量门禁与交付清单（前端）  
13. UI/UX 设计规范与交互标准（对标“美观、易用、人机交互”）  
14. 信创浏览器兼容性与验证清单（对标“稳定性/易用性”）  
15. 平台管理与监控后台（前端）指标与页面落地（对标“运营与监控需求”）  
16. 合规与隐私保护（前端落实）（对标“禁止爬取/隐私合规”）  
17. 前端测试用例与验收清单（可直接用于测试报告）  
附录 A：与后端接口清单映射（前端关注）  
附录 B：赛题条款对标证明材料清单（建议）  

---

## 0. 赛题要求深度对标（前端）

本节以赛题发布页的“功能/非功能/实现条件/提交要求”为准，给出前端侧的**实现策略**与**可验收口径**，用于后续写《测试报告》《设计文档》与答辩自查。

> 说明：赛题中“必须部署在 LoongArch + 银河麒麟 V11/V10”属于整体系统硬性要求。前端作为静态资源（HTML/CSS/JS）本身与 CPU 架构无强绑定，但需要确保 **Nginx/浏览器/字体/渲染能力** 在信创环境可用，并提供兼容性验证材料。

### 0.1 对标总表（前端）
| 赛题要求项 | 赛题要点（摘要） | 前端实现策略（本方案） | 验收口径（可操作） | 证明材料（建议） |
|---|---|---|---|---|
| 架构形态 | B/S 架构，PC Web 或 App | 采用 Vue3 PC Web；同时支持响应式（可作为加分项） | 使用浏览器即可访问；无需本地安装客户端 | 首页/登录页截图；部署访问录屏 |
| 国产环境部署（硬性） | LoongArch + 银河麒麟 V11/V10 | 前端静态 `dist/` + Nginx（LoongArch 版本）部署；不依赖外网 | 在麒麟 V11 服务器通过 Nginx 正常访问；核心流程跑通 | Nginx 配置、部署步骤、访问录屏 |
| 国产数据库（硬性） | DB 需国产数据库 | 前端不直接依赖 DB；通过后端 API 间接访问 | 所有数据交互仅走 `/api/v1/*` | API 联调记录、接口文档 |
| 大模型形态 | 本地大模型或云端大模型均可 | 前端仅展示“模型模式状态”（本地/云端）与异常提示，不在浏览器侧推理 | 管理端可查看模型模式与调用状态（若后端提供接口） | 管理端监控页截图 |
| 双角色注册登录 | 个人/企业分别注册与登录 | 登录成功后依据 `userType` 分流路由与菜单；RBAC 控制按钮/菜单 | 可注册/登录两类账号；进入不同工作台；无越权页面 | 两类账号流程录屏 |
| 文档上传 | 支持上传 DOC/PDF | 上传组件：格式/大小校验、进度条、失败重试、任务追踪（docId） | 可上传 DOC/PDF，50MB 限制前端提示；后端返回 docId | 上传页截图、任务状态录屏 |
| 文档解析结构化 | 自动解析并提取关键信息 | 前端提供：解析进度页、结构化结果页（Tab 分区）、原始 JSON（开发折叠） | 解析中可查询状态；完成后展示结构化字段与证据（若提供） | 解析结果页截图 |
| 能力图谱可视化 | 个人/职位图谱可视化展示 | G6：Force/Hierarchy/Radial；节点详情抽屉；搜索定位；展开收起 | 图谱可缩放/拖拽/搜索；千级节点可用（降级策略生效） | 图谱交互录屏；性能测试截图 |
| 双向匹配与推荐 | 推荐职位/推荐候选人 | 匹配列表 + 详情解释；支持收藏/反馈（如实现） | Top-N 列表可查看详情；解释性字段齐全 | 匹配页截图/录屏 |
| 匹配度评分与依据 | 提供评分与差距依据 | 评分卡片 + 分项拆解 + 技能差距清单 + 雷达图 + 建议 | 评分包含分项；缺失技能/等级差距可见 | 详情页截图 |
| 多维度匹配（加分项） | 城市/意向城市等 | UI 提供“城市/薪资/行业”等筛选与展示位；分项得分可视化 | 能展示多维度加分项（若后端返回） | 匹配详情分项截图 |
| 管理与监控后台 | 用户管理、数据维护、运营监控 | Admin 端：用户/文档/技能库/同义词/匹配记录/监控看板/日志查询 | 管理端菜单完整；关键表格支持检索、分页、批量操作（可选） | 管理端截图集 |
| 非功能：美观大方 | UI 合理、美观 | 统一设计规范（色板/字号/间距/组件规范），一致的状态与交互反馈 | 首页/工作台/图谱/匹配页风格统一；无明显错位 | UI 规范章节 + 截图 |
| 非功能：交互便捷 | 操作便捷、适合用户使用 | 表单校验、引导、空/错/加载态、快捷操作（搜索/回车/一键重试） | 任一核心流程不超过 3 次点击到达关键页 | 交互说明 + 录屏 |
| 非功能：稳定性 | 易用性与稳定性 | 全局错误兜底、重试、超时提示、白屏监控、降级策略 | 接口超时/失败时有提示；不会卡死白屏 | 异常演示录屏 |
| 合规要求 | 禁止爬取/非法采集隐私 | 上传授权勾选、隐私提示；敏感字段脱敏展示；导出/下载权限控制 | 未勾选授权不可提交；敏感字段默认脱敏 | 授权弹窗/脱敏截图 |
| 作品提交物 | 需求/设计/说明书/测试/部署/源码/PPT/视频 | 前端侧输出：设计文档章节 + 测试用例与结果 + 部署说明 + 源码与 dist | 前端材料可拼入“测试报告/部署文档” | 文档目录与证据清单 |

---

## 1. 前端目标与边界

### 1.1 目标
在“LoongArch + 银河麒麟 V11”的信创部署环境约束下，前端以 **静态资源部署** 为主，通过 **Vue 3 + Element Plus + AntV G6** 实现：
- 双角色（个人 / 企业）端到端闭环：注册登录 → 文档上传 → 解析进度 → 图谱查看 → 匹配推荐 → 反馈；
- 管理端：用户 / 文档 / 技能库 / 匹配记录 / 监控面板；
- 图谱可视化与交互：力导向 / 层次 / 辐射等布局，支持拖拽、缩放、搜索、节点展开收起；
- 匹配结果解释：匹配度评分、技能差距与建议可视化（雷达图、条形图等）。

### 1.2 前端边界（与后端协作约定）
前端不直接执行 OCR/LLM/图谱推理/向量检索，仅负责：
- 文件上传与状态轮询/推送；
- 结果展示、交互与“可解释性”UI（证据来源、差距项、学习路径展示）；
- 权限控制与审计 UI（展示日志/监控，不直接采集系统指标）。

---

## 2. 总体前端架构与技术选型

### 2.1 架构分层（前端视角）
前端采用“**UI 表现层** + **业务模块层** + **基础设施层**”的轻量化架构：
- **UI 表现层**：页面（Views）+ 通用组件（Components）；
- **业务模块层**：按领域拆分（auth、document、graph、match、admin），沉淀业务组件与 hooks；
- **基础设施层**：路由、状态管理、API 客户端、权限守卫、日志与埋点（可选）、工具库。

### 2.2 技术栈（建议落地版本）
| 类别 | 组件 | 推荐版本 | 用途 |
|---|---|---:|---|
| 框架 | Vue | 3.4.x | 组合式 API、性能与生态 |
| 构建 | Vite | 5.x | 构建快、配置轻量 |
| 语言 | TypeScript | 5.x | 提升可维护性与契约性 |
| UI | Element Plus | 2.7.x | 组件覆盖全面、适配企业后台 |
| 图谱 | AntV G6 | 5.0.x | 图布局与交互能力强 |
| 图表 | ECharts | 5.5.x | 雷达/柱状/饼图等 |
| 状态管理 | Pinia | 2.1.x | 轻量、与 Vue3 匹配 |
| 路由 | Vue Router | 4.3.x | 页面路由与守卫 |
| HTTP | Axios | 1.7.x | 拦截器、错误统一处理 |
| CSS | UnoCSS（可选） | 0.58.x | 原子化按需生成，控制体积 |
| 代码规范 | ESLint + Prettier | latest | 统一风格 |

### 2.3 信创环境适配原则（前端）
前端以浏览器为运行时，主要风险在于：
- 信创浏览器对现代特性的支持差异（ES2020+、WebGL、字体渲染等）；
- G6 依赖 WebGL/Canvas，需保证显卡驱动与浏览器 WebGL 可用。

因此前端策略：
1. 构建时降级输出：控制目标语法（`build.target`）、必要 polyfill；
2. 运行时能力探测：WebGL 检测，不满足则降级为 Canvas 或简化渲染；
3. 资源体积控制：避免过多依赖引入，启用按需加载与分包。

---

## 3. 前端工程结构与规范

### 3.1 目录结构（建议）
```txt
src/
  assets/                 # 图片、字体、全局样式
  components/             # 通用组件（表格、弹窗、上传组件封装等）
  layouts/                # 布局（双角色主布局、管理端布局）
  views/                  # 页面（按路由）
    auth/
    person/
    company/
    admin/
  modules/                # 业务模块（领域逻辑聚合）
    auth/
    document/
    graph/
    match/
    admin/
  router/                 # 路由与权限守卫
  stores/                 # Pinia store
  api/                    # API 客户端封装（axios 实例、接口）
  types/                  # TS 类型（DTO/VO、Graph 数据结构等）
  utils/                  # 工具（校验、格式化、缓存等）
  hooks/                  # 可复用 hooks（轮询、分页、权限等）
  main.ts
  App.vue
```

### 3.2 约定与规范
- **命名**：组件 PascalCase；文件夹 kebab-case；store `useXxxStore`；
- **业务模块拆分优先**：避免所有逻辑堆在 views；
- **类型先行**：与后端 DTO/VO 形成对应关系，关键接口类型必须落到 `src/types`；
- **错误统一处理**：Axios 响应拦截器把错误规范化，页面只处理“业务态”。

---

## 4. 页面与功能模块设计（双角色 + 管理端）

### 4.1 页面地图（建议）
| 角色 | 一级菜单 | 二级页面 | 核心能力 |
|---|---|---|---|
| 通用 | 认证 | 注册/登录/找回 | token 获取、角色路由分流 |
| 个人 | 简历中心 | 简历上传、解析进度、解析结果 | 上传 DOC/PDF、轮询进度、展示结构化信息 |
| 个人 | 能力图谱 | 个人能力图谱 | G6 渲染、搜索、展开、证据查看 |
| 个人 | 职位推荐 | 推荐列表、详情、对比 | 匹配度、差距、学习路径、收藏/反馈 |
| 企业 | JD 中心 | 职位上传、解析进度、解析结果 | 上传 JD 文档，提取技能要求 |
| 企业 | 职位图谱 | 职位能力图谱 | 以职位为中心的技能网络 |
| 企业 | 候选人推荐 | 推荐列表、详情、对比 | 候选人匹配解释与差距 |
| 管理端 | 系统管理 | 用户/角色/权限 | RBAC 配置、禁用/重置 |
| 管理端 | 数据维护 | 文档库/技能库/同义词 | CRUD + 审核流（可选） |
| 管理端 | 运营监控 | 服务状态/模型状态/日志 | 状态看板、查询与导出（可选） |

### 4.2 关键页面交互要点
1. **上传页**：支持拖拽、大小/格式校验、上传进度条；上传后进入“解析任务视图”；  
2. **解析任务视图**：展示任务状态（排队/处理中/完成/失败），支持重试；  
3. **图谱页**：提供布局切换、搜索定位、节点详情抽屉、关系高亮；  
4. **匹配页**：展示 Top-N 列表 + 解释性详情（评分拆解、差距项、建议）；  
5. **管理端**：表格 + 条件筛选 + 批量操作 + 审计记录入口。

---

## 5. 路由、布局与权限体系（RBAC）

### 5.1 路由分组
建议按角色与端拆分路由：
- `/auth/*`：登录、注册；
- `/person/*`：个人端；
- `/company/*`：企业端；
- `/admin/*`：管理端（仅管理员）。

### 5.2 布局策略
- `AuthLayout`：极简布局；
- `MainLayout`：个人/企业端通用（顶部用户信息 + 侧边菜单 + 内容区）；
- `AdminLayout`：管理端（多级菜单、面包屑、标签页可选）。

### 5.3 权限模型（前端）
后端使用 Sa-Token RBAC，前端配合：
1. 登录后获取 `token` + `userType`（person/company/admin）+ `permissionCodes`（可选）；
2. 路由守卫进行：
   - 未登录 → 跳转登录；
   - 已登录但角色不匹配 → 403 页面；
   - 细粒度权限（按钮级）→ `v-permission` 指令或组合式函数控制显示/禁用。

#### 5.3.1 路由守卫伪代码
```ts
router.beforeEach(async (to) => {
  const auth = useAuthStore()
  if (to.meta.public) return true
  if (!auth.token) return { name: 'Login', query: { redirect: to.fullPath } }
  if (to.meta.userTypes && !to.meta.userTypes.includes(auth.userType)) return { name: 'Forbidden' }
  return true
})
```

### 5.4 Token 存储建议
在信创内网环境下仍需防范 XSS：
- 优先：token 存内存 + 刷新机制（需要后端支持 refresh token）；  
- 可接受：`sessionStorage`（关闭浏览器即失效）；  
- 不建议：长期 `localStorage` 明文保存。

---

## 6. API 接入层与数据模型（TypeScript）

### 6.1 API 规范对齐（来自总体方案）
- 基础路径：`/api/v1/`
- 认证：`Authorization: Bearer {token}`
- 响应包装：
```json
{ "code": 200, "message": "success", "data": {} }
```

### 6.2 Axios 实例与拦截器（建议）
```ts
// src/api/http.ts
import axios from 'axios'
import { useAuthStore } from '@/stores/auth'

export const http = axios.create({
  baseURL: import.meta.env.VITE_API_BASE,
  timeout: 15000,
})

http.interceptors.request.use((config) => {
  const auth = useAuthStore()
  if (auth.token) config.headers.Authorization = `Bearer ${auth.token}`
  return config
})

http.interceptors.response.use(
  (resp) => {
    const body = resp.data
    if (body?.code !== 200) return Promise.reject(body)
    return body.data
  },
  (err) => Promise.reject(err),
)
```

### 6.3 核心类型定义（示例）
```ts
// src/types/api.ts
export interface ApiResp<T> {
  code: number
  message: string
  data: T
}

export type UserType = 'PERSON' | 'COMPANY' | 'ADMIN'

export interface LoginResp {
  token: string
  userType: UserType
  userId: string
  permissions?: string[]
}

export type DocType = 'RESUME' | 'JOB_DESC'
export type DocStatus = 'UPLOADING' | 'PENDING' | 'PROCESSING' | 'DONE' | 'FAILED'

export interface DocFileVO {
  id: string
  fileName: string
  fileType: 'DOC' | 'PDF'
  docType: DocType
  status: DocStatus
  createdAt: string
}

export interface ParseResultVO {
  docId: string
  resultJson: any
  status: DocStatus
}
```

---

## 7. 文档上传与解析进度前端方案

### 7.1 上传策略
支持 DOC/PDF，限制 50MB：
- 前端校验：文件后缀 + MIME（仅作提示，最终以后端校验为准）；
- 采用分片上传：**可选项**（后端支持则开启；若不支持则使用普通上传）；
- 上传完成后返回 `docId`，进入解析任务跟踪。

### 7.2 解析任务跟踪（轮询方案）
在资源受限单体后端中，优先使用轮询（而非 WebSocket）：
- 轮询接口：`GET /api/v1/document/{id}/status`
- 轮询间隔：1s（处理中）→ 3s（排队中）→ 10s（长时间未完成）自适应退避；
- 终止条件：DONE/FAILED。

建议封装 `usePolling`：
```ts
// src/hooks/usePolling.ts
export function usePolling<T>(fn: () => Promise<T>, intervalMs = 2000) {
  let timer: number | null = null
  const start = () => { stop(); timer = window.setInterval(fn, intervalMs) }
  const stop = () => { if (timer) window.clearInterval(timer); timer = null }
  return { start, stop }
}
```

### 7.3 解析结果展示
接口：`GET /api/v1/document/{id}/result`  
前端展示策略：
- 原始 JSON：提供“开发模式”折叠面板（便于联调）；
- 结构化表单：按“技能/经历/教育/项目”等 Tab 分区；
- 证据链：展示“证据来源”（文档页码/段落，若后端提供）。

---

## 8. 能力图谱可视化方案（AntV G6 5.x）

### 8.1 图谱数据接口与数据格式
建议后端输出 G6 友好的结构（nodes/edges），减少前端转换成本：
```ts
// src/types/graph.ts
export interface GraphNode {
  id: string
  label: string
  type: 'Person' | 'Job' | 'Skill' | 'Project' | 'Company' | 'Category'
  props?: Record<string, any>
}

export interface GraphEdge {
  id: string
  source: string
  target: string
  type: 'HAS_SKILL' | 'REQUIRES_SKILL' | 'DEPENDS_ON' | 'BELONGS_TO' | 'APPLIED_IN' | 'PUBLISHES'
  props?: Record<string, any>
}

export interface GraphData {
  nodes: GraphNode[]
  edges: GraphEdge[]
}
```

推荐接口：
- `GET /api/v1/graph/person/{id}` → 个人能力子图；
- `GET /api/v1/graph/job/{id}` → 职位能力子图；
- `POST /api/v1/graph/search` → 节点搜索与定位（返回 nodeId 或子图）。

### 8.2 视觉编码（样式规范）
| 节点类型 | 形状 | 主色 | 说明 |
|---|---|---|---|
| Person | 圆形/头像节点 | #1677ff | 个人中心 |
| Job | 圆角矩形 | #fa8c16 | 职位中心 |
| Skill | 圆形 | #52c41a | 技能点 |
| Project | 菱形 | #722ed1 | 项目经验 |
| Company | 矩形 | #13c2c2 | 企业实体 |
| Category | 标签/胶囊 | #8c8c8c | 分类体系 |

边样式建议：
- `REQUIRES_SKILL`：实线；必需加粗/红边；加分项虚线；
- `DEPENDS_ON`：箭头方向明确，颜色更浅；
- hover 高亮：聚焦 1-hop 邻居，其它降低透明度。

### 8.3 布局与交互
提供布局切换：
- Force（全局浏览/聚类发现）
- Dagre/Hierarchy（分类体系/依赖链）
- Radial（以人/职位为中心）

核心交互：
- 拖拽画布、节点拖拽；
- 滚轮缩放；
- 框选/点选；
- 节点详情抽屉（props 展示 + 关联列表）；
- 搜索定位：输入技能/节点名 → 高亮并自动居中；
- 节点展开/收起：对超千节点建议“按层加载”。

### 8.4 大图性能策略（千级节点）
1. **服务端裁剪**：默认只返回 1~2 跳子图；  
2. **增量加载**：点击节点再加载邻居（lazy expand）；  
3. **渲染降级**：超过阈值时：
   - 关闭阴影/复杂渐变；
   - 文本仅在一定缩放级别显示；
   - 使用聚合（cluster）/折叠（collapse）节点；  
4. **离屏与节流**：resize、hover、search 高亮均需节流。

### 8.5 WebGL 可用性检测与降级
若目标浏览器 WebGL 不可用：
- 降级为 Canvas 渲染；
- 限制布局为层次/辐射，减少 force 迭代；
- 降低节点数量（强制更小子图）。

---

## 9. 智能匹配结果呈现方案（ECharts）

### 9.1 页面结构（推荐）
- 匹配列表（Top-N）：排序、筛选、分页；
- 匹配详情：
  - 总分与分项得分（卡片 + 进度条）；
  - 技能覆盖度（已满足/缺失/加分项）；
  - 雷达图：个人能力 vs 职位要求；
  - 技能差距清单：缺失技能、最低等级差距；
  - 学习路径建议：图谱推理路径可视化（列表/迷你图）。

### 9.2 雷达图数据模型（示例）
```ts
export interface MatchDetailVO {
  recordId: string
  score: number
  scoreBreakdown: {
    skillCoverage: number
    levelMatch: number
    vectorSim: number
    graphReasoning: number
    cityBonus?: number
    salaryBonus?: number
  }
  matchedSkills: Array<{ name: string; requiredLevel: number; personLevel: number }>
  missingSkills: Array<{ name: string; requiredLevel: number; gap: number }>
  suggestions?: string[]
}
```

---

## 10. 性能、兼容性与安全加固（信创浏览器）

### 10.1 性能目标（前端侧）
- 首屏可交互：≤ 2s（内网）；
- 图谱渲染：千级节点可用（交互不卡死）；
- 匹配页面：分页查询/筛选响应流畅。

### 10.2 构建优化
- 路由级懒加载：`import()`；
- 依赖分包：将 `g6`、`echarts` 独立 chunk；
- 图片/字体压缩与缓存策略；
- 关闭生产 sourcemap（或仅内网保留）；
- 组件按需：Element Plus 按需引入。

### 10.3 兼容性策略
- Vite 构建 target 降级（示例：`es2018` 或更低，视信创浏览器内核而定）；
- 必要 polyfill（如 Promise、URL、AbortController 视情况引入）；
- CSS 兼容：避免过新的选择器/特性，必要时加 postcss 处理；
- WebGL 检测与降级（见 8.5）。

### 10.4 安全建议
- 前端输入校验（但不替代后端校验）；
- 防 XSS：不使用 `v-html` 渲染不可信内容；如需渲染富文本，必须做 sanitize；
- 防 CSRF：如采用 token header 方式，默认风险较低；仍建议后端校验 Origin/Referer；
- 文件上传：仅显示允许类型，前端不解析文件内容（避免攻击面扩大）。

---

## 11. 构建、离线依赖与部署（Nginx）

### 11.1 环境变量
建议通过 `.env.*` 管理：
```bash
# .env.production
VITE_API_BASE=/api/v1
VITE_APP_TITLE=AI智能匹配与能力图谱系统
```

### 11.2 Nginx 部署（静态资源 + 反向代理）
前端最终交付为 `dist/`，部署到银河麒麟服务器 Nginx：
```nginx
server {
  listen 80;
  server_name _;

  root /opt/app/front/dist;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }

  # 反向代理后端（单体 Spring Boot）
  location /api/ {
    proxy_pass http://127.0.0.1:8080;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
  }
}
```

### 11.3 离线部署注意事项（前端）
由于离线环境可能缺少 Node 生态依赖下载能力：
- 推荐方式 A：在有网环境完成 `pnpm install && pnpm build`，将 `dist/` 拷贝到离线服务器；
- 推荐方式 B：准备离线 npm 仓库/制品库（如 Verdaccio）+ 离线依赖包；
- Node 在 LoongArch 可用性需提前验证（若不可用，优先采用方式 A）。

---

## 12. 测试、质量门禁与交付清单（前端）

### 12.1 测试建议
- 单元测试（可选）：对 `utils/hooks/api` 做最小覆盖；
- E2E（可选）：覆盖登录、上传、查看结果、查看图谱、查看匹配详情；
- 兼容性测试：奇安信/360 信创版浏览器至少各测试 1 次核心流程；
- 性能测试：千级节点图谱加载与交互体验确认。

### 12.2 质量门禁
- ESLint/TS 类型检查必须通过；
- 构建产物体积阈值（建议）：首屏资源 ≤ 2.5MB（gzip 后更小）；
- 关键异常兜底：白屏捕获（`window.onerror`/`unhandledrejection`）+ 错误提示页。

### 12.3 前端交付清单（建议）
1. 前端源码（含 README、环境变量示例）  
2. `dist/` 构建产物（离线可直接部署）  
3. Nginx 配置样例  
4. 信创浏览器兼容性说明与**验证清单/结果截图**（见第 14 章）  
5. 核心页面截图与录屏素材（登录、上传、图谱、匹配、管理端）  
6. 《赛题条款深度对标表（前端）》与证明材料索引（见第 0 章与附录 B）  
7. 前端测试用例与验收清单 + 实测结果（可直接纳入测试报告，见第 17 章）  

---

## 13. UI/UX 设计规范与交互标准（对标“美观、易用、人机交互”）

本章的目标是把“界面美观大方、交互便捷、适合用户使用”落成**可执行规范**，避免只写“建议”。

### 13.1 统一设计语言（Design Token 建议）
建议在 `src/assets/theme/` 固化基础设计变量（即使不引入完整设计系统，也要做到一致）。

**色彩（示例）**
- 主色：#1677ff（强调/主按钮/链接）
- 成功：#52c41a（成功状态）
- 警告：#faad14（排队/提示）
- 危险：#ff4d4f（失败/必需缺失）
- 中性色：#262626/#595959/#8c8c8c（文本层级）

**字号与间距（示例）**
- 标题：20/18/16；正文：14；辅助：12
- 页面内边距：24；卡片间距：16；表单项间距：12

### 13.2 通用状态规范（必须实现）
| 状态 | 表现 | 统一要求 |
|---|---|---|
| Loading | Skeleton / Spin | 列表与详情均需骨架屏；避免白屏等待 |
| Empty | 空状态插画 + CTA | 给出下一步操作：去上传/去搜索/去创建 |
| Error | 错误提示 + 重试 | 展示可读原因（网络/权限/服务忙），提供重试按钮 |
| No Permission | 403 页面 | 清晰提示 + 返回首页/切换账号 |
| Offline/Timeout | 超时提示 | 支持“重试/稍后再试”，避免用户无反馈 |

### 13.3 表单与上传交互规范
1. **注册/登录**：手机号/邮箱格式校验；密码强度提示；回车提交；失败给出明确原因；  
2. **上传前授权**：必须勾选“已获得简历/职位文档授权、同意隐私条款”才能提交（对标合规）；  
3. **上传过程**：显示文件名、大小、类型、进度条；失败可重试；成功后自动进入任务页；  
4. **上传后不可重复提交**：同一文件短时间重复提交需二次确认（防误操作）。  

### 13.4 表格与列表规范（管理端与推荐列表）
- 默认列宽、溢出省略 + 悬浮显示完整；
- 支持条件筛选与分页（pageNum/pageSize）；
- 关键字段提供“复制”按钮（如 docId、recordId），便于联调与排障；
- 批量操作需二次确认（禁用用户/删除数据/重置等）。

### 13.5 图谱交互细则（必须落地）
| 交互点 | 规范 | 目的 |
|---|---|---|
| 进入图谱默认视图 | 默认 Radial（以人/职位为中心）或 Hierarchy（分类） | 降低首次理解成本 |
| 搜索定位 | 支持技能名模糊匹配；定位后高亮 1-hop 邻居 | 快速找到目标 |
| 节点详情 | 右侧抽屉：属性 + 关联列表 + 证据（如有） | 可解释性 |
| 展开/收起 | 超阈值节点自动折叠；点击再加载邻居 | 千级节点可用 |
| 布局切换 | Force/Hierarchy/Radial 切换时提示“重新计算布局” | 避免误解卡顿 |
| 性能保护 | 节点>1000 提示“建议缩小范围”，并给出操作入口 | 防卡死 |

### 13.6 响应式与移动端（满足“PC Web 或 App”扩展）
本期以 **PC Web** 作为交付形态；同时建议：
- 登录/上传/列表页支持响应式（≥ 1024 为桌面；< 1024 为平板/移动简化布局）；
- 图谱页移动端可提供“只读简化模式”（禁用复杂交互，避免性能与误触）。

---

## 14. 信创浏览器兼容性与验证清单（对标“稳定性/易用性”）

### 14.1 兼容性目标与测试矩阵
建议至少覆盖以下浏览器（以赛方环境为准，名称仅示例）：
| 类别 | 浏览器 | 必测模块 |
|---|---|---|
| 信创浏览器 | 奇安信浏览器（信创版） | 登录、上传、图谱、匹配、管理端 |
| 信创浏览器 | 360 信创浏览器 | 同上 |
| 系统自带 | 麒麟默认浏览器/Chromium 变体 | 同上 |

### 14.2 构建输出与降级策略（建议写入工程配置）
1. **Vite target 降级**：根据目标内核设定（例如 `es2018`），避免过新的语法导致白屏；  
2. **按需 polyfill**：仅引入必要 polyfill（Promise/URL/AbortController/crypto 等视实际而定）；  
3. **分包与懒加载**：`g6`、`echarts`、`element-plus` 分离 chunk，降低首屏压力；  
4. **字体与图标**：尽量使用本地可用字体，Icon 采用 SVG（避免字体文件兼容问题）。  

### 14.3 WebGL/Canvas 运行时探测与降级
图谱渲染在信创环境常见风险是 WebGL 不可用或性能不稳定：
- 进入图谱页时执行 WebGL 探测：失败则提示“已切换兼容模式（Canvas/简化布局）”；  
- 兼容模式限制：禁用 Force 强迭代，默认 Hierarchy/Radial；减少阴影/渐变；限制节点数；  
- 强制子图：若节点数量超过阈值，提示用户“缩小范围/按条件过滤”。  

### 14.4 兼容性验收清单（可直接抄进测试报告）
| 验收项 | 步骤 | 预期结果 |
|---|---|---|
| 登录 | 输入账号密码→登录 | 正常进入对应角色工作台 |
| 注册 | 注册个人/企业账号 | 表单校验正常；注册成功可登录 |
| 上传 DOC/PDF | 上传 1 份 ≤50MB 文件 | 校验提示正确；上传成功返回 docId |
| 解析进度 | 进入任务页轮询 | 状态可更新；DONE/FAILED 正确结束 |
| 解析结果展示 | 打开结果页 | Tab 分区展示；异常字段有兜底显示 |
| 图谱渲染（WebGL） | 打开图谱、缩放拖拽搜索 | 不卡死；交互生效；可查看节点详情 |
| 图谱降级（无 WebGL） | 模拟/实际关闭 WebGL | 自动进入兼容模式；仍可浏览关键关系 |
| 匹配列表与详情 | 打开推荐→查看详情 | 分项得分、差距、雷达图可见 |
| 管理端权限 | 普通用户访问 /admin | 被拦截并提示 403 |
| 管理端核心表格 | 用户/文档/技能库 | 查询、分页、空/错态正常 |

---

## 15. 平台管理与监控后台（前端）指标与页面落地（对标“运营与监控需求”）

赛题要求“满足基本运营与监控”，前端侧需要把“看什么、怎么看、多久刷新一次”明确下来。

### 15.1 监控看板信息架构（建议）
**页面：运营监控 / MonitorDashboard**
- 概览卡片（实时/近 24h）：  
  - 文档解析：总任务数、成功率、平均耗时、失败数  
  - 匹配服务：调用次数、平均响应、Top 错误码  
  - 图谱：节点总量（或增长量）、查询次数、平均查询耗时  
  - 模型：当前模式（本地/云端）、最近一次健康检查时间、可用性  
- 趋势图（按小时/天）：解析耗时、失败率、匹配调用量  
- 告警/事件列表：最近错误、服务不可用事件（如后端提供）  

### 15.2 指标定义（前端展示口径）
| 指标 | 含义 | 展示形式 | 刷新建议 |
|---|---|---|---|
| parse_success_rate | 解析成功率 | 百分比 + 趋势 | 30s~60s |
| parse_avg_ms | 解析平均耗时 | 数值 + 趋势 | 30s~60s |
| parse_fail_top_reason | 失败原因 Top | 条形图/列表 | 60s |
| match_avg_ms | 匹配平均响应 | 数值 + 趋势 | 30s~60s |
| model_mode | 本地/云端 | 状态 Tag | 10s~30s |
| service_health | 服务健康状态 | 红黄绿 | 10s~30s |

> 若后端暂未提供“监控聚合接口”，前端可先按 mock 结构预留 UI，占位不影响评审（评审更看重“有监控能力”）。

### 15.3 日志审计与导出（对标“基本运营”）
管理端建议提供：
- 操作日志查询：按用户、时间、模块、结果筛选；
- 审计详情抽屉：展示请求摘要（不含敏感原文）、结果码、耗时；
- 导出：仅管理员可导出，导出内容默认脱敏（见第 16 章）。

---

## 16. 合规与隐私保护（前端落实）（对标“禁止爬取/隐私合规”）

### 16.1 上传授权与合规提示（必须）
在上传简历/职位文档前，强制用户确认：
- “本人/本单位已获得该文档相关主体授权”
- “不上传包含非法获取的个人隐私数据”
- “同意隐私条款与数据使用说明”

**验收口径**：未勾选授权复选框 → 上传按钮禁用，且有明确提示文案。

### 16.2 敏感信息默认脱敏展示（建议）
对可能出现的敏感字段（手机号、邮箱、身份证、住址等）：
- 默认展示脱敏格式（例：`138****1234`）；
- 提供“查看原文”入口，但需要二次确认或管理员权限（视赛题要求与实现成本决定）。

### 16.3 导出/下载控制（建议加分）
若有“导出匹配报告/解析结果”的需求：
- 默认禁止普通用户导出其他用户数据；
- 导出文件可加水印（用户名/时间/用途），降低外泄风险；
- 对频繁导出/下载提示并可触发验证码（可选，若后端支持）。

### 16.4 防误用与反爬提示（前端可做的部分）
赛题强调“严禁爬取或非法采集”，前端侧可配合：
- 关键接口操作增加节流（例如搜索/下载）；
- 明确展示“数据合规声明”与违规后果提示（在上传页与系统说明页）。

---

## 17. 前端测试用例与验收清单（可直接用于测试报告）

本章给出“用例模板 + 核心用例集”，你们后续只需补“实际结果/截图/日期/测试人员”即可形成《软件功能测试报告》的前端部分。

### 17.1 用例模板（建议）
| 用例编号 | 模块 | 用例名称 | 前置条件 | 操作步骤 | 预期结果 | 实际结果 | 备注/截图 |
|---|---|---|---|---|---|---|---|
| FE-AUTH-001 | 认证 | 个人用户登录 | 已有账号 | 输入账号密码→登录 | 进入个人工作台 |  |  |

### 17.2 核心功能用例集（覆盖赛题 5 大模块）
#### A. 双角色注册与文档上传
- FE-AUTH-001 个人登录  
- FE-AUTH-002 企业登录  
- FE-AUTH-003 未登录访问受限路由拦截  
- FE-DOC-001 上传 PDF（≤50MB）  
- FE-DOC-002 上传 DOC（≤50MB）  
- FE-DOC-003 上传超 50MB 提示与阻止  
- FE-DOC-004 上传失败提示与重试  
- FE-DOC-005 未勾选授权无法上传（合规）  

#### B. 文档解析与结构化展示
- FE-PARSE-001 解析进度轮询正常更新  
- FE-PARSE-002 解析完成后可查看结果  
- FE-PARSE-003 解析失败展示原因与重试入口  
- FE-PARSE-004 结构化字段缺失时页面不崩溃（兜底）  

#### C. 能力图谱可视化
- FE-GRAPH-001 个人图谱加载与渲染  
- FE-GRAPH-002 职位图谱加载与渲染  
- FE-GRAPH-003 搜索定位与高亮邻居  
- FE-GRAPH-004 节点详情抽屉展示属性  
- FE-GRAPH-005 节点展开/收起与增量加载  
- FE-GRAPH-006 WebGL 不可用时降级可用（兼容模式）  
- FE-GRAPH-007 千级节点场景交互不卡死（性能保护策略生效）  

#### D. 智能匹配与推荐
- FE-MATCH-001 推荐列表展示与分页  
- FE-MATCH-002 匹配详情评分卡片与分项拆解  
- FE-MATCH-003 技能差距清单正确展示  
- FE-MATCH-004 雷达图渲染正常  
- FE-MATCH-005 反馈提交成功提示  

#### E. 管理与监控后台
- FE-ADMIN-001 管理端权限拦截（非管理员 403）  
- FE-ADMIN-002 用户管理列表/查询/分页  
- FE-ADMIN-003 文档库查询与状态展示  
- FE-ADMIN-004 监控看板卡片与趋势图展示（或占位提示）  
- FE-ADMIN-005 日志审计查询与详情抽屉  

### 17.3 非功能验收（前端侧）
- FE-NFR-001 首屏可交互 ≤ 2s（内网环境）  
- FE-NFR-002 接口超时/断网提示清晰，不白屏  
- FE-NFR-003 兼容性：完成第 14 章清单全项验证并留存截图  

---

## 附录 A：与后端接口清单映射（前端关注）
| 模块 | 接口 | 用途 |
|---|---|---|
| 认证 | POST /api/v1/auth/register | 注册 |
| 认证 | POST /api/v1/auth/login | 登录 |
| 认证 | POST /api/v1/auth/logout | 登出 |
| 文档 | POST /api/v1/document/upload | 上传并触发解析 |
| 文档 | GET /api/v1/document/{id}/status | 查询解析进度 |
| 文档 | GET /api/v1/document/{id}/result | 获取解析结果 |
| 图谱 | GET /api/v1/graph/person/{id} | 个人图谱 |
| 图谱 | GET /api/v1/graph/job/{id} | 职位图谱 |
| 图谱 | POST /api/v1/graph/search | 搜索定位 |
| 匹配 | POST /api/v1/match/recommend-jobs | 个人推荐职位 |
| 匹配 | POST /api/v1/match/recommend-candidates | 企业推荐候选人 |
| 匹配 | GET /api/v1/match/{id}/detail | 匹配详情 |
| 匹配 | GET /api/v1/match/history | 历史 |
| 匹配 | POST /api/v1/match/feedback | 反馈 |

---

## 附录 B：赛题条款对标证明材料清单（建议）
用于你们最终提交时快速打包材料（尤其“文档与演示”占比 20%）。

| 材料类型 | 建议内容 | 用途 |
|---|---|---|
| 截图 | 登录/注册、上传、解析进度、解析结果、图谱、匹配详情、管理端 | 文档插图、答辩展示 |
| 录屏 | 两类用户全流程各 1 条；图谱交互 1 条；异常兜底 1 条 | 演示视频素材 |
| 配置 | Nginx 配置、env 配置、构建命令 | 部署文档 |
| 验证结果 | 信创浏览器兼容性验证表（第 14 章） | 测试报告 |
| 用例与结果 | 第 17 章用例集的“实际结果/截图” | 测试报告 |
| 对标表 | 第 0 章对标表 + 证据链接/截图编号 | 自查与答辩 |
